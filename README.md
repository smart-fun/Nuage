# Nuage
Nuage is a lightweight, local-first Android database that automatically syncs with the user's personal cloud (Google Drive, Dropbox) using JSON batches. It abstracts SQLite complexity, providing a simple, transaction-based API for seamless data persistence and cross-device sync.
